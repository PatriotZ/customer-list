<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Akhtari's Customer List</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">
</head>
<body>
    <h1>Akhtari's Customer List</h1>
    <% if(message !== "success") { %>
        <h1>We Encountered an Error</h1>
        <p><%= message %></p>
    <% } else { %>
        <h2>There are <%= model.length %> customers in the database</h2>
        <table class="table">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">Customer ID</th>
                    <th scope="col">Fist Name</th>
                    <th scope="col">Last Name</th>
                    <th scope="col">Customers State</th>
                    <th scope="col">Current Sales</th>
                    <th scope="col">Previous Sales</th>
                </tr>
            </thead>
            <tbody>
                <% model.forEach(customer => { %>                   
                    <tr>
                        <td><%= customer %></td>
                        <td><%= customer.cusFname %></td>
                        <td><%= customer.cusLname %></td>
                        <td><%= customer.cusState %></td>
                        <td><%= customer.cusSalesYTD %></td>
                        <td><%= customer.cusSalesPrev %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    <% } %>
</body>
</html>